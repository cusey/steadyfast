<%= form_for(dictionary) do |f| %>
  <% if dictionary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dictionary.errors.count, "error") %> prohibited this dictionary from being saved:</h2>

      <ul>
      <% dictionary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %>
    <%= f.text_field :word %>
  </div>

  <div class="field">
    <%= f.label :partOfSpeech %>
    <%= f.text_field :partOfSpeech %>
  </div>

  <div class="field">
    <%= f.label :definition %>
    <%= f.text_field :definition %>
  </div>

  <div class="field">
    <%= f.label :wordUsage %>
    <%= f.text_field :wordUsage %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
